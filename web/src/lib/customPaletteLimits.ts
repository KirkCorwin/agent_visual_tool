/** Max custom types on palette 1 (master). */
export const MAX_CUSTOM_TYPES_PALETTE_1 = 30;

/** Max custom types on palettes 2–4. */
export const MAX_CUSTOM_TYPES_PALETTE_SECONDARY = 40;

/** Max user-defined custom palette types per graph (all panels combined). */
export const MAX_CUSTOM_PALETTE_TYPES =
  MAX_CUSTOM_TYPES_PALETTE_1 +
  3 * MAX_CUSTOM_TYPES_PALETTE_SECONDARY;
