export {
  ACCESSIBLE_CATEGORICAL_SLOTS,
  ACCESSIBLE_PALETTES,
  NODE_TYPE_LABELS,
  STANDARD_NODE_TYPE_COLORS as NODE_TYPE_COLORS,
  resolveCustomDisplayColor,
  resolveNodeBorderColor,
  resolveNodeTypeColors,
  resolveNodeTypeLabel,
  toggleAccessibleColorMode,
} from "../../graph/nodeColors";
